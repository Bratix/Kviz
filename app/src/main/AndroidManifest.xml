<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amar.kviz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Dodaj_Pitanje"
            android:label="Dodaj pitanje"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Kviz"
            android:label="@string/title_activity_kviz"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Bodovi"
            android:label="@string/title_activity_bodovi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Lista_Igraca"
            android:label="@string/title_activity_lista__igraca"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>